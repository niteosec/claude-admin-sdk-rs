# Fixtures

`docs/` holds response bodies hand-built from Anthropic's Managed Agents API reference as fetched on
2026-09-19: every documented field, placeholder values only, not live captures. Replace each with a
redacted live capture when one exists.
