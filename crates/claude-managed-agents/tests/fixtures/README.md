# Fixtures

The files under `docs/` are hand-built from Anthropic's Managed Agents API reference (fetched 2026-09-19), with every documented field and placeholder values only. They are not live captures; replace them when real responses are observed.
